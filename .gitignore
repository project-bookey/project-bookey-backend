# ── OS / editor
.DS_Store
.idea/
.vscode/
*.iml

# ── Java / Maven
target/
!.mvn/wrapper/maven-wrapper.jar
.mvn/timing.properties

# ── Node (도구 스크립트용)
node_modules/


# ── env / secrets
.env
.env.*
!.env.example
*.pem
*.p8
*.keystore
google-services.json
GoogleService-Info.plist

# ── 로컬 업로드 파일 (bookey.storage.type=local)
server/uploads/

# ── logs / tmp
*.log
logs/
tmp/
coverage/

# ── 로컬 작업 노트 (커밋 금지)
.superpowers/

