-- ============================================================
--  bookey V16 — 선호 카테고리 (온보딩)
--  가입 직후 온보딩에서 고른 관심 분야. 추천·피드 개인화의 입력값이 된다.
-- ============================================================

ALTER TABLE users ADD COLUMN preferred_categories TEXT[] NOT NULL DEFAULT '{}';
